var assert = require("assert");


describe('Saving', function() {
    describe('test 1', function() {
        it('should save the state of a textbox into an array', function(){
            assert.equal(toSave(SOMETHING), truth);
        })
    });
});